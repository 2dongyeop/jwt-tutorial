spring:

  datasource:
    url: jdbc:h2:tcp://localhost/~/jwt-tutorial
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
#        show_sql: true

  sql:
    init:
      mode: always
#      continue-on-error: true # ?? ??? sql?? ??? ? ?? ???? ?? ??
#      data-locations: classpath:data.sql # ?? ??? dml sql?? ??? ?? ? ?? ??

logging:
  level:
    io.wisoft: DEBUG
